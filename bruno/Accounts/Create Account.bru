meta {
  name: Create Account
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts
  body: json
  auth: inherit
}

headers {
  Prefer: return=representation
}

body:json {
  {
    "name": "New Account"
  }
}
